/**
 * @deepseek-ai/dsh-plugin-chaos — Chaos plugin bundle.
 * The bundle patch (cordis.patch.yml) composes all chaos plugins over the
 * web-app layer. This plugin body is empty: the bundle is a distribution
 * format for config rows, not runtime code.
 * @module @deepseek-ai/dsh-plugin-chaos
 */

/** Bundle plugin body — composition happens through the patch layer. */
export function apply(): void {}
